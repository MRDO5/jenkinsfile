pipeline {
     agent any 
     stages {
        stage('Build'){
                steps {
                  sh 'mvn clean packeg'
                }
		post {
		   success { 
			echo 'New Archiving...'
			archiveArtifacts artifacts: '**/*.war'
        	  }
     		}
  	}
     }
}
